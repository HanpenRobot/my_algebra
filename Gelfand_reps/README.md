線形作用素の環
ベクトル空間XからXへの線形作用素の全体
```math
A=Hom(X,X)
```

Aにベクトル算法を
```math
(ag+bg)(x)=af(x)+bg(x) ,(f,g \in A, a,b \in K)
```
と定義して、Aをベクトル空間と考えると

```math
x: f \mapsto y=f(x)
```
の考え方(線形写像自身に空間の構造を規定する役割を担わせる考え方)で

```math
Hom(A,X)=X
```

とみなせる。